const radius = 5.0;
const fontSize = 15.0;

const firstGroup = 'group=0';

// database
const dateBox = 'date_box';
const dateKey = 'date_key';

const itemBox = 'item_box';
const itemKey = 'item_key';

const typeBox = 'type_box';
const typeKey = 'type_key';

const themeBox = 'theme_box';
const themeKey = 'theme_key';
